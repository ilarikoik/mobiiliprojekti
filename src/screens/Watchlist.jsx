import React from 'react';
import { View, Text, Button } from 'react-native';

export default function Watchlist({ navigation }) {
  return (
    <View>
      <Text>katselu Screen</Text>
      {/* <Button
        title="Go to Home"
        onPress={() => navigation.navigate('Leffat')}
      /> */}
    </View>
  );
}
